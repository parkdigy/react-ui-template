import { type FormControlGroupContextValue } from './FormControlGroupContext.types';

const FormControlGroupContext = React.createContext<FormControlGroupContextValue>({} as any);

export default FormControlGroupContext;
